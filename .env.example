VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional: JSON jobs feed for /swipe (defaults to local mocks when unset).
# Expected shape: JobListing[] or { "jobs": JobListing[] }
# See src/lib/jobs.ts and docs/WEDGE.md — no scrapers.
# VITE_JOBS_API_URL=https://example.com/api/jobs
